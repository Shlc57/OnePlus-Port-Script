# ColorOS 钱包 NFC 运行时设置自动补写（features/fix_coloros_wallet）。
# 钱包 isNfcEseMode 依赖 settings global nfc_multise_active == "Embedded SE"，
# 缺失时开门/乘车弹"设为默认 NFC 应用"对话框。该键由欧加 Nfc 写入而小米
# Nfc_st 不写；data 分区设置无法在镜像侧写入，只能开机后由 init exec 补写。
# 机制说明：保留 shell 域以复用其 `/system/bin/sh` entrypoint 与读 odm 脚本的许可，
# 但 uid 必须是 root：DSU 实机验证 HyperOS 下 uid shell(2000) 调用 settings 会被
# SettingsProvider 拒（`SecurityException: must have WRITE_SECURE_SETTINGS`），而该拒绝
# 只取决于 uid（uid 2000 在 shell 域与 ksu 域同样被拒；uid 0 在两域均成功），与 SELinux 无关。
# uid system(1000) 配 shell 域会触发另一个 NullPointerException，不可用。
# 每次开机幂等覆写同一值；补写完成后延迟通知钱包刷新缓存（小米 Nfc 不发该 Oplus 广播）。
on property:sys.boot_completed=1
    exec_background u:r:shell:s0 root root -- /system/bin/sh /odm/etc/init/coloros_wallet_nfc_settings.sh
