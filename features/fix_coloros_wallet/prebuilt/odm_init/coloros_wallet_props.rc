# ColorOS 钱包运行时身份键（features/fix_coloros_wallet）。
# 原包 system build.prop 将 ro.product.brand/manufacturer 污染为小米值，且
# 分区键 ro.product.odm.brand=Xiaomi 由 fix_device_identity 有意保留（小米生态）。
# init 在 post-fs-data（早于 zygote 启动）直接 setprop 覆盖全局值，使 app 进程
# fork 后的 Build.BRAND 为 OnePlus，越过 FinShell 厂商门（判定链 i.c/i.d 与
# zygote 预加载的 static final 缓存）。机制同 odm/etc/init/xiaoai_wakeup_props.rc。
on post-fs-data
    setprop ro.product.brand OnePlus
    setprop ro.product.manufacturer OnePlus
    setprop ro.build.version.oplusrom V16.0.9
    setprop ro.build.version.oplusrom.display 16.0
    setprop ro.product.cuptsm "ONEPLUS|ESE|01|02"
    setprop ro.vendor.oplus.regionmark CN
    setprop persist.sys.oplus.region CN
    setprop persist.sys.oppo.region CN
