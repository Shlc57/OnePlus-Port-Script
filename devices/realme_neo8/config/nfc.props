# 真我 Neo8 组合流程为目标 NFC 选择的 Xiaomi 上层兼容开关（ro.vendor.nfc.*）。
# 由 features/fix_nfc_tms_bridge 消费（与一加 Ace 6 共用）：Neo8 底包为青藤 THN31（TMS 栈），
# 桥接保留小米 MIUI 签名的 Nfc_st（含其自带 NCI 栈）+ /dev/tms_nfc 节点别名 + TMS HAL SELinux，
# 本文件只提供写入 odm/build.prop 的上层兼容开关，不从小米原包复制目标硬件属性。
# 不再用 NXP 专用 features/fix_nci_nfc（底包缺 NXP HAL 契约）；通用 Transsion NfcNci 因
# android.uid.nfc 绑 MIUI 密钥、外部签名+v3 验签失败，无法在 HyperOS 装用。NFC 点亮仍需真机验证。
ro.vendor.nfc.repair=1
ro.vendor.nfc.wallet_fusion=1
ro.vendor.nfc.secure_display_optim=1
ro.vendor.nfc.mitouch=1
ro.vendor.nfc.phonecase=1
